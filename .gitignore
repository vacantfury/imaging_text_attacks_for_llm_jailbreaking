# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.env
# Local secret-injection launcher (a wrapper that feeds env-var values from a
# secret manager; never holds secret values itself — keep out of this public repo):
/run
.venv
env/
venv/
ENV/

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Jupyter
.ipynb_checkpoints/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# MyPy
.mypy_cache/

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Local config
*.local




# Project-specific
.cursorignore
.claude/
human_and_llm_agent_interaction/
human_and_agent_interaction
scripts/
literature/
logs/
test/
private_files/
paper/
backup_codes/
backup_files/
test.py
poetry.lock
TODO.md
other_repos/
keys/
fonts/
AI_agent/
temporary_scripts/
# Confidential reviewer text — must NEVER be committed (repo is public)
text_docs/reviews/

# Idea-stage paper drafts / external idea-check writeups (kept local until ready)
new_papers/

# Hydra
multirun/
outputs/

# MLflow
mlruns/
mlartifacts/

# Data — ignore large dataset files, keep directory structure
data/original_datasets/
data/processed_datasets/
# generated BoN pilot variant sets (local; regenerable via scripts/expand_bon_dataset.py)
data/*_bon_pilot.jsonl
# Provenance breadcrumb: current commit SHA, auto-written by the post-commit
# hook so cluster runs (no .git there) still record a real git_sha.
.git_sha

# Round J human-label sheets (ground-truth labels, kept local)
judge_model_issue/

# Cluster ops/property docs + local key material (personal, never public)
cluster_files/

# Multi-cluster dispatch: real pool config carries cluster host/user/account
# specifics (local-only; conf/cluster_pool.example.yaml is the committed template).
conf/cluster_pool.yaml
# Generated per-cluster sub-presets (written by dispatch.py under the paper subdir).
conf/experiment/_mc_*.yaml
conf/experiment/**/_mc_*.yaml

# Auto-research strategy docs — personal strategy content (charter, blueprint), local-only like TODO.md
text_docs/shared/auto_research_pattern.md
text_docs/shared/auto_research_charter.md

# roadmap/planning doc — local-only (kept out of the public repo)
text_docs/shared/future_work.md

# web-research notes (visualization tools, etc.) — local only, not papers
knowledge/

# Private career-weighted venue planning (personal strategy — never commit)
text_docs/venue_value.md

# ── Analyzed results — NOT published (owner 2026-07-26) ───────────────────────
# The repo publishes CODE and DESIGN, never raw outputs or analyzed results.
# `outputs/` (raw) was already ignored; these are the ANALYZED layer, which had
# been tracked by oversight since the repo's early commits.
text_docs/bestofn_attack/experiment_results.md
text_docs/bestofn_attack/experiment_matrix.md
text_docs/autoattack_defense/experiment_results.md
text_docs/autoattack_defense/experiment_matrix.md
text_docs/autoattack_defense/experiments_findings.md
text_docs/autoattack_defense/progress_report.md
text_docs/defense_read_access/experiment_results.md
text_docs/defense_read_access/experiment_matrix.md
text_docs/image_presence_threshold/experiment_matrix.md
text_docs/image_presence_threshold/paper_b_rebuttal_results.md
text_docs/bestofn_defense/r4_gate_record.md
text_docs/bestofn_defense/idea_2026-07-13.md
NOW.md

# task-estate store + operational state (personal data — never committed; task-estate refactor 2026-08-04)
/state/
