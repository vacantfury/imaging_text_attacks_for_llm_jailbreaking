# ============================================================
# Paper C (AS-3) · METHOD-FIDELITY RERUN — STAGE 2 slice: qwen x WildGuard  (2026-08-05)
# ============================================================
# Consumes Stage 1's re-encoded dirs (NURC job 8957825, verified: `appendleft`
# gone and `.append(` present on 200/200 CodeAttack rows; FigStep carrying its
# canonical instruction with 100 images per axis).
#
# WHY THIS SLICE AND NOT THE FULL GRID: the full two-column grid is ~90 cells and
# wants 6 servers per target (1 target + 5 guards). AICR is full (AS-4 R11/R10)
# and NURC's GPU nodes are down/drained, so the grid is capacity-blocked. This
# slice needs 2 servers and fits xc's free slots. Do NOT "make the grid fit" by
# lowering num_cluster_jobs: the replica allocator is FLOOR DIVISION, so many
# distinct models against a small budget silently collapses every model to one
# replica (recorded lesson: project_replica_allocator_floor_division).
#
# FULL n=100, not a pilot slice. The target responses are the expensive artifact
# and they are judge-independent, so running the full 100 now means the reportable
# pass is a pure `rejudge` over stored responses at ZERO target cost. A 20-prompt
# pilot would have to be re-run in full later.
#
# JUDGES ARE CHEAP-FIRST AND NOT REPORTABLE:
#   harmful -> wildguard, benign -> qwen (OR-Bench 3-class), both free on cluster.
#   ⚠️ WildGuard is INVALID as an ASR judge — it flags harmful INTENT rather than
#   task completion and over-flags encoded/code attacks by 41-68%. NO number from
#   this preset goes in the paper. The reportable pass is a gpt-5-mini rejudge,
#   which is API spend and a separate owner decision.
#
# `campaign` is set so these cells can be PINNED during reduction. Latest-wins
# selection is unsafe here — the quarantined pre-fix cells share every other key
# (recorded lesson: project_cell_selection_latest_wins_is_unsafe).
#
# Servers: qwen (target + OR-Bench judge) + wildguard (guard + harm judge) = 2.
#   -> num_cluster_jobs = orchestrator + 2 = 3.
# ============================================================

num_main_job_threads: 4
num_cluster_jobs: 3

_mc: &mc {guard_model: wildguard, decode_text: true, decode_style: recover}

_harm: &harm
  mode: defense+evaluate
  target_model: qwen2_5_vl_7b
  prompt_range: [0, 99]
  judge_method: wildguard
  judge_model: wildguard
  campaign: paper_c_fidelity_rerun

_ben: &ben
  mode: defense+evaluate
  target_model: qwen2_5_vl_7b
  prompt_range: [0, 99]
  benchmark: orbench_benign_hard
  judge_method: orbench
  judge_model: qwen2_5_vl_7b
  campaign: paper_c_fidelity_rerun

# ── Stage-1 sources (NURC job 8957825, 2026-08-05; mirrored to hub and xc) ──
_code_h: &code_h outputs/autoattack_defense/prompt_transform/harmbench/code_attack_20260805_215649_22270770/code_attack
_fig_h:  &fig_h  outputs/autoattack_defense/prompt_transform/harmbench/ir_figstep_20260805_215649_72871103/ir_figstep
_code_b: &code_b outputs/autoattack_defense/prompt_transform/orbench_benign_hard/code_attack_20260805_215649_90917535/code_attack
_fig_b:  &fig_b  outputs/autoattack_defense/prompt_transform/orbench_benign_hard/ir_figstep_20260805_215649_9632753/ir_figstep

tasks:
  # ── HARMFUL axis · CodeAttack (§1.1) ──
  - {<<: *harm, defense: no_defense,        source_transform_subdir: *code_h}
  - {<<: *harm, defense: guard_baseline,    source_transform_subdir: *code_h, defense_config: {guard_model: wildguard}}
  - {<<: *harm, defense: modality_complete, source_transform_subdir: *code_h, defense_config: *mc}

  # ── HARMFUL axis · FigStep (§1.3) ──
  - {<<: *harm, defense: no_defense,        source_transform_subdir: *fig_h}
  - {<<: *harm, defense: guard_baseline,    source_transform_subdir: *fig_h, defense_config: {guard_model: wildguard}}
  - {<<: *harm, defense: modality_complete, source_transform_subdir: *fig_h, defense_config: *mc}

  # ── BENIGN axis (over-refusal) · CodeAttack ──
  - {<<: *ben, defense: no_defense,        source_transform_subdir: *code_b}
  - {<<: *ben, defense: guard_baseline,    source_transform_subdir: *code_b, defense_config: {guard_model: wildguard}}
  - {<<: *ben, defense: modality_complete, source_transform_subdir: *code_b, defense_config: *mc}

  # ── BENIGN axis (over-refusal) · FigStep ──
  - {<<: *ben, defense: no_defense,        source_transform_subdir: *fig_b}
  - {<<: *ben, defense: guard_baseline,    source_transform_subdir: *fig_b, defense_config: {guard_model: wildguard}}
  - {<<: *ben, defense: modality_complete, source_transform_subdir: *fig_b, defense_config: *mc}
