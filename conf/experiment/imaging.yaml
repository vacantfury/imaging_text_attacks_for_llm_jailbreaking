# ============================================================
# Stage 2: Render encoded prompts as images (Plain baseline)
# ============================================================
# Usage: python main.py imaging
#
# Renderer params loaded from conf/imaging/plain.yaml automatically.
# Override individual params in the 'renderer:' block if needed.

tasks:
  - mode: imaging
    renderer_type: plain
    source_dir: outputs/encode_plain_YYYYMMDD_HHMMSS

  - mode: imaging
    renderer_type: plain
    source_dir: outputs/encode_math_YYYYMMDD_HHMMSS

  - mode: imaging
    renderer_type: plain
    source_dir: outputs/encode_classical_chinese_YYYYMMDD_HHMMSS
