# Evaluation defaults for Encoding × Modality project
# Dual-judge: GPT-5-nano (HarmBench protocol) + Llama Guard 3

judge_method: harmbench          # "harmbench", "jailbreakbench", "refusal", or "orbench"

judge_llm_config:
  model: gpt-5-nano
  max_tokens: 16384
  temperature: 0.0
