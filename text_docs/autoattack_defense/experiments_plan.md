# Experiments Plan — Coverage-Complete Defense · execution playbook

Companion to `proposal.md`. Cluster/open-weight VLMs are primary; closed API models are a late breadth layer. **Data collection freezes before the writing phase.** Run rounds in order; each round has a **gate** that decides whether/how to proceed.

> **▶ ACTIVE-PRESET CONVENTION (read first):** the only experiment file that is ever run is **`conf/experiment/experiment.yaml`** — via `sbatch scripts/run_experiment.sbatch experiment` (cluster) or `python main.py experiment` (local). **For every round/step, OVERWRITE `experiment.yaml`** with that round's tasks. Do **not** create separate per-round preset files (e.g. `c1_stage2.yaml`) and expect them to run — they won't; `experiment` is hard-coded as the run target. Keep prior rounds' YAML in git history, not as extra files.

> **▶ PROJECT SCOPE (read second):** this plan executes **only the current project** — the coverage-gap measurement (RQ1) and the coverage-complete guard (RQ2–RQ4). Compound attacks, cross-modal splitting, the `joint_verify` defender, mechanism probes, and API breadth are **Future Work** (proposal §11). Some of that code is already built (see §2) — it stays in the repo but is **not run** by this plan.

---

## 0. Status — where we are (done ✓ / next ▶)

**Done — do NOT redo:**
- ✓ 4 target models chosen; **all weights downloaded** to `/scratch/$USER/huggingface_cache` (job 7307844).
- ✓ Llama-3.2-11B-Vision + InternVL3-8B **wired** into `llm_model.py` (+ `family`/`alignment_tier`) and `conf/llm/{llama3_2_11b_vision,internvl3_8b}.yaml` (InternVL3 has `trust_remote_code: true`). Commit `5f283e3`.
- ✓ Experiment-infra refactor (commits `944f7b9`→`6bf960d`): every `results.json` now carries `git_sha`/`git_dirty`/`schema_version`/`judge_config_hash`/`status`, named `metrics`+`primary_metric` (no `asr` overload), first-class `model_family`/`alignment_tier`/`companion_image_path`, `upstream_ref` pointer, atomic writes. Tables read these fields — **filter by `schema_version`** to exclude any stray legacy dir.
- ✓ Paper ImgAug's old `outputs/` archived to `backup_files/arr_may_submission_files/outputs/`; `outputs/` is empty and current-project-only.
- ✓ **OCR-fidelity probe built as a standalone tool** (`temporary_scripts/ocr_probe.{sbatch,py}`): serves each VLM serially, transcribes sampled `ir_plain` images vs upstream encoded text, prints per-image fidelity + SUMMARY. NURC HTTP-proxy connectivity for loopback health-check/inference **fixed** (the manager's `ProxyHandler({})` pattern → `no_proxy`/`--noproxy` in the probe sbatch).
- ✓ **Image renderer reworked (2026-06-05):** `ir_plain` now renders at a **fixed readable font and paginates long content across multiple images** — was single-image *shrink-to-fit*, which made long encodings (esp. `code_attack`, ~13k chars) OCR-illegible. Multi-image plumbed end-to-end: schema `image_encoded: list[str]` (back-compat str→[str]), all 4 provider services already accept an image list, defenses load all pages. Over-budget prompts (> `MAX_PAGES_PER_PROMPT=8` pages) are **excluded + flagged `is_within_maxlen=False` / `num_images` in `raw_results`**, never truncated. `page_height=4096` keeps typical prompts to 1–3 images (code_attack→2, math→1). `--max-model-len` kept at 16384 (32768 risks V100 KV-OOM on Pixtral-12B). Old single-image renders demarcated in `experiment_results.md`.
- ✓ **Multi-surface baseline + ArtPrompt built (2026-06-11):** `amia_ia` defender (AMIA's *intention-analysis* component only — masking omitted as out-of-threat-model; faithful reconstruction, since AMIA has no released code) and `non_llm_artprompt` encoder (faithful port of `other_repos/ArtPrompt`; dependency-free `gen` font + optional `art`-lib fonts). Both registered + smoke-verified. See `code_dev_plan.md`. These add the **decode-gap** thread to the plan (Round 3).
- ✓ **Mechanism-error exclusion + max-model-len bump (2026-06-11):** any target call that fails to produce a valid output (query-time context-overflow, network/timeout/rate-limit, failed batch item) is flagged `is_correctly_processed=False` and **excluded from the ASR denominator** (not miscounted as a defeated attack); a refusal is a *successful* call and stays counted. `--max-model-len` **raised 16384 → 20480** (`conf/llm/default.yaml` — supersedes the "16384" in the renderer note above); covers most `code_attack ir_plain` overflows, the rest excluded by the mechanism above. (Found via job `mj_7455981` triage; see `experiment_results.md` Notes & caveats.)
- ✓ **Homoglyph encoder built (2026-06-12):** `non_llm_homoglyph` — reuses the `homoglyphs` library (cite **Boucher et al. 2022, *Bad Characters***, `9833641` — NOT home-grown). Registered + smoke-verified. The **normalization** decode axis (vs the *semantic* axis of set_theory/formal_logic), so it broadens the decode-gap across decode *types*. Optional external-validity add, like ArtPrompt; not in the core suite. ⚠️ **TEXT-channel only** (OCR would un-homoglyph it).
  - **⛔ GATE — before running any homoglyph *defense* cell:** `modality_complete` decodes *semantics*, not Unicode, so homoglyph would **defeat our own guard and refute the headline**. First add an `NFKC` + `homoglyphs.to_ascii` **normalize step to `modality_complete`'s recover stage** (`code_dev_plan.md` §6). **Until that lands, homoglyph is `no_defense`-only** (the bare-target gate: does the model even read through the confusables? if not, the cell is dead). The encoder is built; the guard normalize step is **NOT** — do not run sage/amia_ia/modality_complete × homoglyph until it is.
- **Classical-Chinese encoder added to the suite (2026-06-16):** `llm_classical_language` (already built; reused from prior multimodal work). Rationale: it is a **third decode *type*** — *linguistic / translation* — alongside the *semantic-math* (set_theory/formal_logic), *code* (code_attack), and *normalization* (homoglyph) types, so it broadens the decode-gap across types and strengthens **RQ4 held-out generalization** (a held-out encoder of a different family). It is **short + OCR-legible**, so unlike `code_attack` it is valid on **both** the text and image (`ir_plain`) channels. ⚠️ **Decode-gap caveat (watch at G3):** Classical Chinese is a *language the target reads natively*, so an inspect-only defense (`sage`/`amia_ia`) may catch the harm by *understanding* it — without an explicit decode step. That is **informative either way**: if it slips past inspection → another decode-gap confirmation across a new type; if inspection catches it → it marks the boundary of the decode-gap claim (semantic/algorithmic encodings, not natural languages). Run it `no_defense` first (does the encoding even land an attack on each model?), then through the defense matrix like set_theory/formal_logic.

**⛔ DROPPED — do NOT run / do NOT consider (2026-06-11):**
- **`code_attack` × `ir_plain` (image-rendered code_attack), ALL models — problematic, excluded from the suite.** Two compounding, unfixable-cheaply problems: (1) the ~13k-char render **overflows context even at 20480** on heavy-image-token models (qwen2.5-vl, pixtral) — fully fixing needs a GPU-risky `max_len` (32768 → Pixtral KV-OOM) or denser rendering (→ OCR-illegible); (2) on lighter models it is **OCR-illegible** (InternVL3 `code_attack` OCR ≈0.10, old renderer), so a low ASR is *the attack failing to transmit*, not a defense — **uninterpretable**. **Decision: `code_attack` is TEXT-channel only; its `ir_plain` cell is excluded from the coverage map.** The image surface is carried by `set_theory`/`formal_logic ir_plain` (OCR-legible, no overflow). Any stray `code_attack ir_plain` number (e.g. InternVL3 24%) is flagged *suspicious — do not use* in `experiment_results.md`. **Still run normally:** `code_attack` **text** (clean, 77% qwen) and `set_theory`/`formal_logic` on all channels.

**⛔ NOT ACTUALLY DONE — verified 2026-07-11.** A full `outputs/` sync shows **zero** `sage` / `amia_ia` / `modality_complete` runs anywhere — only 3 June-5 `no_defense` C0 baselines + the 104 July-10 Paper-B rebuttal runs (`ecso`/`no_defense` only). The `experiment.yaml` referenced below was long since overwritten by the rebuttal config. Conclusion: the 2026-06-12 decode-gap gate produced **no saved outputs — Paper C's defense experiments have NOT been run**; only C0 baselines exist. **Round 1 must be launched fresh.**

**▶ Round 1 (`mj_8291382`) — DONE + triaged 2026-07-11.** Run clean (36/36, errors benign). KEY FINDING: on the text channel `modality_complete` had **NO decode step** — it ran SAGE over the still-ENCODED text, so it was ≈ `sage` by construction (near-identical numbers). So Round 1 did NOT refute the decode-gap thesis — it exposed a naive implementation. `modality_complete` **REFINED** (commit `b034c09`): added a text-decode step (config-gated `decode_text`, default on). Round-1 raw numbers valid but NOT recorded to `experiment_results.md` (superseded by the refined re-run; record once the design-vs-capability question resolves). Other verified reads: `no_defense` ASR 45–84% (attacks land); internvl3's 0% cells are REAL refusals; `amia_ia`'s low ASR is reframe-luck ("no harmful intent, proceed" → benign math), not detection.

**▶ Round 1b (`mj_8295436`) — DONE + triaged + recorded 2026-07-11.** RESULT: the refined `modality_complete` decode step WORKS — ASR ~0–5% on open models, dominating `sage`/`amia_ia`; Round-1's "mc fails" was entirely the missing text-decode step (naive design). Decode is essential for weak open VLMs, less so for capable gemini (catches encoded harm natively). **Gate G3 (TEXT channel) PASSED** — see `experiment_results.md` "Round 1/1b — Decode-gap gate". Grid + caveats there.
**▶ Round 2img (`mj_8296134`, campaign `paper_c_image`) — DONE + triaged + recorded 2026-07-11.** RESULT: the IMAGE-channel modality claim **PASSES** — `mc-on` (RECOVER+DECODE(union)+GUARD) collapses ASR to **0–3%** on all 6 cells, dominating `no_defense` (38–50%) and `ecso` (22–51%); raw-verified genuine decode-then-refuse, run clean (24/24, 0/2400 error-or-empty, 0 HTTP-400). Decode's marginal value is model-dependent (large on compliant qwen `mc-off` 20–25 → `mc-on` 1–3; small on cautious internvl/pixtral, `mc-off` already 0–4% via surface-cue refusal) — mirrors the text channel. `ecso` > `no_defense` on pixtral (caption normalizes harm). **Both channels now pass** (text = Round 1/1b, image = here). Grid + caveats in `experiment_results.md` "Round 2img". **Next make-or-break: over-refusal COST (Round 2 benign) — the one number this round deliberately omits (HarmBench runs the harm judge only; refusal fields null by design, so benign must be measured with a benign benchmark + refusal judge).**
**▶ Round 3 (`mj_8303802`, campaign `paper_c_round3_amplifier`) — DONE + triaged + recorded 2026-07-12.** RESULT: the REFRAMED amplifier thesis **HOLDS** — guardreasoner *alone* on raw encoded/imaged input blocks only 0–15% (ASR 37–78%); prepend recover+decode and the SAME guard blocks 49–80%, ASR → **7–23%** on symbolic encoders (set_theory/formal_logic) across BOTH channels. `code_attack` is the **bounded exception** (amplifier weak: ASR 29–59%, model-dependent — guardreasoner surfaces code-embedded harm poorly even after decode). guardreasoner served first try; 45/45 `success`; parser sane (low guard_baseline block rates); mechanism raw-verified (block-rate delta). Grid + caveats in `experiment_results.md` "Round 3". Design/provenance retained for the record: The REFRAMED headline (commit `6e07023`): recover+decode as an AMPLIFIER prepended to an OFF-THE-SHELF guard. 45 cells (HarmBench 0–99): `{no_defense, guard_baseline[guardreasoner_vl_7b], modality_complete[guard_model=guardreasoner_vl_7b, decode]}` × `{set_theory, formal_logic, code_attack (text); set_theory, formal_logic (ir_plain)}` × `{qwen2_5_vl_7b, internvl3_8b, pixtral_12b}`. **FIRST real cluster run** of the guard serve-plumbing + `guard_baseline` / mc-external-guard machinery (commit `cc3c503`) + `guardreasoner_vl_7b` (a Qwen2.5-VL-7B fine-tune → low serve risk; wildguard/llama_guard_4 EXCLUDED tonight, validated separately). Budget: 4 gpu servers = MaxJobsPU cap exactly (submitted with 0 competing jobs), orchestrator on `short`. **Watch-points all CLEARED at triage:** (1) guardreasoner SERVED (no hang); (2) `guard_baseline[gr]` misses (blocks 0–15%) where `modality_complete[gr]` catches (blocks 49–80%) — confirmed; (3) `parse_guardreasoner_vl` sane (guard_baseline not over-blocking); (4) ran clean in ~1h46m, 45/45 `success`, 0 empty/mis-processed; (5) in-run `no_defense` reproduced the Round-1b/2img baseline shape.
⏭️ Remaining risks after this: over-refusal COST (Round 2 benign-refusal), adaptive/harder encoders + cross-modal splitting, positioning (decode vs modality framing), consensus rejudge for headline numbers.

Historical claim (the phantom 2026-06-12 run) retained below for the record:

**▶ JUST RAN (2026-06-12) — awaiting triage:** the current `conf/experiment/experiment.yaml` — a **text-channel decode-gap gate**: `{set_theory, formal_logic, code_attack(text)}` × `{no_defense, sage, amia_ia, modality_complete}` × `{qwen2_5_vl_7b, internvl3_8b, pixtral_12b}` = **36 cells** (HarmBench, rows 0–99). First real run of `amia_ia` + `modality_complete`, and Pixtral's serving test. Pulled the decode-gap gate early on the *clean* text channel (no overflow/OCR confounds). **Next action: triage it (`check-experiment-results`)** — watch for (a) whether `sage`/`amia_ia` reasoning *decodes* the encodings (if they do, the decode-gap differentiation is weak — proposal §6 P5), (b) per-defense errors on the two new defenders, (c) whether Pixtral served.

**Next ▶:** triage the above → Round 1 coverage gap → Round 2 guard + cost → **Round 3 decode gap (image + multi-surface)** → Round 4 generalization. (Direction set 2026-06-06: current project is the **coverage-complete defense**; attack-side work moved to Future Work. Decode-gap differentiation added 2026-06-11.)

**✅ RESOLVED 2026-07-15 — JUDGE SELECTED (the Round-J bake-off is done).** Final judge: **`gpt-5-mini`** as the single headline ASR judge (best *justifiable* HarmBench-rubric judge, κ=0.68 vs the 62y/38n human anchor), with the full 26-judge panel — **`WildGuard`** featured — as the robustness/calibration view; apply gpt-5-mini + the HarmBench rubric uniformly to HB **and** JBB. This is a **SHARED resolution** — it applies to every HarmBench-judged paper (B/C/D), not only C. Full report (problem · design · κ table · criteria + leniency findings · guards'-papers justification · HB/JBB · caveats · lift-ready bottom line): **`judge_model_issue/JUDGE_MODEL_REPORT.md`**; shared summary: `text_docs/shared/judge_validation_sample.md`. **Remaining is execution, not an open question:** re-judge the coverage cells with gpt-5-mini (they still carry nano numbers); optional small JBB validation slice. Original problem statement (kept for context):

**JUDGE METHODOLOGY (plan fixed 2026-07-12; problem opened 2026-07-11):** the entire coverage map's *absolute* ASR rests on the `gpt-5-nano` judge, and the Paper-B rebuttal work **confirmed** this judge inflates absolute ASR ~2–3× vs Claude/Gemini judges — the direction held but the *level* did not. See `experiment_results.md` Results C / C-strong. Same judge on the same encoded-attack family Paper C reports → a live Paper-C problem.
- **DECISION (settled 2026-07-12) — VALIDATE, don't adopt.** No benchmark ships a fresh, strong, *human-validated* judge for our exact setting (image-rendered **encoded** attacks on VLMs — confirmed by the 2026-07-12 lit search, `literature_review.md` §7.5), and HB/JBB are reputable, so **keep HB + JBB datasets** and fix only the *judge*. The judge is settled by **human calibration on OUR data**, not by importing an external judge (a benchmark can't decide it for us, and adopting a rubric ≠ picking a model). **⚠️ USABILITY GATE — screen this FIRST, before accuracy (owner-recalled 2026-07-12):** many safety-aligned API models **REFUSE the judging task itself** — their filter flags the harmful content in what they're asked to judge, so they emit no verdict and are useless as a judge. `gpt-5-nano` is confirmed usable (that's *why* it was the incumbent); many stronger API models are NOT. This is the **strongest reason the project now LEANS open-source** (owner 2026-07-12): self-served open models are **steerable** (you own the system prompt; no external refusal layer), and the purpose-built safety judges (`harmbench`-cls / `llama_guard` / `wildguard` / `guardreasoner`) are **refusal-immune by construction** (they emit a verdict; refusal isn't in their output space).
  0. **Usability screen** — run each candidate on a small sample; drop any that refuse / won't emit a parseable verdict on ≥~90% of it. Only usable candidates proceed to κ.
  1. Owner **hand-labels a small stratified sample** of our own stored responses (~40, oversampling borderline + inter-judge-disagreement cases). A few is enough for a κ with an honest CI — it's a spot-check, not a judge paper.
  2. **Bake off** candidate judges against those human labels; keep the **best TWO** — human agreement (κ) is the PRIMARY selector, **different model families** (independent blind spots), mutual consistency only a secondary tiebreak. (Beware two judges that agree with each other but both inflate.)
  3. **Candidate pool:** API {gpt / gemini / grok / deepseek / z.ai} — **Claude excluded (too expensive as a judge)** — PLUS **open-source judges** (reproducible + free on the cluster, a real methodological plus): the purpose-built ones we ALREADY serve (`harmbench` classifier, `llama_guard_3`/`4`, `wildguard`, `guardreasoner`) + general open models with a rubric — **NOT limited to 1-GPU / small models** (owner 2026-07-12): capable LARGE open models are candidates and arguably BEST for judging encoded harm (capability drives recall), serveable via tensor-parallel on our A100-80GB/H200 nodes — from `gpt-oss-120b` (1×80GB) / Qwen3-32B (1 GPU) up to ~600–700B-class (DeepSeek-V3.2 / GLM-4.6 / Qwen3-235B / Llama-4-Maverick / Mistral-Large-3) on **one full H200 node**. The judge pass is DECOUPLED (only the judge runs, no targets) so a whole node can go to one big judge. Plus the published safety judges (StrongREJECT-Gemma-2B, SORRY-Bench-Mistral, SALAD MD-Judge). `gpt-5-nano` is a candidate too (known-usable), **droppable if it fails**. DeepSeek/Z.AI/xAI providers wired 2026-07-12 (commit `b6c97f2`); judge calls carry no personal data → mainland endpoints are fine (data-jurisdiction rule).
  4. **StrongREJECT is NOT adopted** (text-only → can't be our benchmark; and its "judge" is a rubric, not a model). **Cite it only** for the methodology point — existing judges overstate ASR, and a usefulness-rubric beats binary refusal (`literature_review.md` §7.5).
  - **OPEN — decide at WRITING time (NOT settled):** whether `gpt-5-nano`'s numbers stay in the paper as a documented spread/reference. Discuss when drafting.
  - **⚠️ Reminder:** if a **Z.AI or DeepSeek** model makes the final two, notify the owner to **top up that account** before the re-judge run.
- **Publishability floor (do not block the paper on a perfect judge):** the paper stands **even without** a clean judge solution — the effect **direction is robust across judges**; only absolute ASR is fragile. Fallback framing: report under a defensible judge + caveats, and lean the claims on **relative contrasts + statistical significance** (Wilson CIs, McNemar — already validated on the rebuttal cells) rather than headline absolute ASR. Aim to solve it; ship regardless.

---

## 1. Fixed pieces (reference)

| Axis | Values |
|---|---|
| **Targets** | `qwen2_5_vl_7b` (workhorse), `pixtral_12b`, `llama3_2_11b_vision`, `internvl3_8b` — all NU-cluster, 1–2 GPU each |
| **Encoders** (Stage 1, exist) | `set_theory`, `formal_logic`, `code_attack`, **`classical_chinese`** (`llm_classical_language` — added 2026-06-16; a *linguistic* decode type vs. the semantic-math of set_theory/formal_logic, and **OCR-legible / short**, so it carries **both** the text and image channels like set_theory/formal_logic — unlike `code_attack`, which is text-only). |
| **Image transforms** (Stage 1, exist) | `ir_plain` (render attack into image — **fixed-font, paginated multi-image**), `constant_image` (decoy; `params.image_path`), `blank_image` |
| **Attack suite (current project)** | the existing encoders × image transforms above, used as **single-method portfolio** (best-of-suite) queries. **No new attacks built this project.** · *Optional external-validity:* `non_llm_artprompt` (published ASCII-art encoding; built) — a third-party encoding for the decode gap, held out of the core suite; **TEXT-channel only** (never → `ir_plain`). |
| **Defenses (Stage 2)** | `no_defense`, `sage`, `ecso`, `decoy`-lever, **`semantic_smooth`** (well-known smoothing baseline — paraphrase ×N + majority vote; *meaning-preserving so it never decodes* → weak vs encoding, but valuable to beat because it is well-known; **coupled defense** — its own `defense` mode + `perturbation_model`, default `gemini-2.5-flash-lite`), **`modality_complete` (the contribution)** · **Multi-surface tier (closest prior art):** `amia_ia` (AMIA intention-analysis only, masking omitted — **the decode-gap baseline**); **BlueSuffix = structural-only, NOT run** (code exists → faithful-or-nothing). · **REMOVED — do NOT reference:** `eta` (dropped `1bc0899`) and `mllm_protector` (deleted 2026-06-16, both file + `conf/defense/mllm_protector.yaml`) — the output-axis baseline is **out of scope** for this paper. |
| **Benchmarks** | HarmBench-harmful (rows 0–99) = ASR; JailbreakBench-benign (0–99) = utility |
| **Judge** | `gpt-5-nano` (API), ImgAug-parity; HarmBench + JBB-refusal evaluators |
| **Decoding** | temp 0, top_p 1, seed 42 |

**How to run a stage** (preset = `conf/experiment/<name>.yaml`):
```bash
sbatch scripts/run_experiment.sbatch <preset>     # cluster (auto-serves vLLM targets)
python main.py <preset>                            # local smoke (API targets only)
```
**Pipeline:** Stage 1 `prompt_transform` (render attack chains, model-independent) → Stage 2 `defense+evaluate` (consumes a Stage-1 step subdir via `source_transform_subdir`; applies defense, queries target, judges). Stage 1 is cheap and shared across all targets — render once, reuse.

---

## 2. Build status (code) — ✅ all built, registered, smoke-verified, committed

| Item | Status | Where | Used this project? |
|---|---|---|:--:|
| **`modality_complete` defender** (the contribution) | ✅ `35897be` | `src/defense/modality_complete.py` — RECOVER image→text, then one SAGE-style check over the *union* of both channels, eyes-closed | **YES** |
| ~~**`eta` / `mllm_protector` (output-axis baselines)**~~ | ⛔ **REMOVED** | `eta` dropped `1bc0899`; `mllm_protector` deleted 2026-06-16 (file + `conf/defense/mllm_protector.yaml` + factory import) — never run, output-axis is out of scope | **NO — gone** |
| **`semantic_smooth` defender** (smoothing baseline) | ✅ built (pre-existing, Paper B) | `src/defense/semantic_smooth.py` — paraphrase ×N + majority vote; **coupled** (`defense` mode + `perturbation_model`, default `gemini-2.5-flash-lite`) | **YES — baseline to beat** |
| **`joint_verify` defender** | ✅ `35897be` | `src/defense/joint_verify.py` — judge the joint (text+image) request, then answer-or-refuse | **Future Work** (§11.2) |
| **`ecso_evade` attack** | ✅ `6c26a74` | `src/prompt_transformations/text/ecso_evade.py` — output-framing wrapper | **Future Work** (compound) |
| **`cross_modal_split` attack** | ✅ `6c26a74` | `src/prompt_transformations/image/cross_modal_split.py` — scaffold (positional split) | **Future Work** (§11.2) |
| **`amia_ia` defender** (multi-surface baseline) | ✅ 2026-06-11 | `src/defense/amia_ia.py` — AMIA intention-analysis pass only (masking omitted); image passthrough; parses `[Final Response]`, logs raw `[Intention Analysis]` + marker-absence | **YES — decode-gap baseline** |
| **`non_llm_artprompt` encoder** (external-validity) | ✅ 2026-06-11 | `src/prompt_transformations/text/encoders/non_llm_artprompt_encoder.py` — faithful ArtPrompt port; `gen` no-dep / `art`-lib fonts; `font` is the sweep knob (use `cards`/`alphabet`, not `gen`, for a clean probe) | optional (external validity) |

All resolve via `create_defense(...)` / `create_transformation(...)` and pass parse+construct+apply smoke tests. **For this project the headline defender is `modality_complete`** (+ the baselines `sage`/`ecso`/`decoy`/`semantic_smooth` and the `amia_ia` decode-gap baseline). The output-axis baselines (`eta`, `mllm_protector`) have been **removed from the repo** — output-axis checking is out of scope here. The attack-side code (`ecso_evade`, `cross_modal_split`) and `joint_verify` are **built but reserved for Future Work** — do not run them in this plan. Reporting follows the **coverage map** in proposal §1/§4.4/§8 (every cell is a finding — never claim a "sweep").

---

## 3. Rounds (do in order)

### Round 0 — Serve + smoke + OCR fidelity check  ▶ START HERE
**Goal:** confirm the serving models can *read rendered text* (else the image-channel cells are impossible on them).
**Steps:**
1. Overwrite `experiment.yaml` with a few `defense+evaluate`/`no_defense` tasks on `llama3_2_11b_vision` + `internvl3_8b` over ~4 prompts, one text-only and one with an `ir_plain` image of a benign sentence. `sbatch scripts/run_experiment.sbatch experiment`.
2. **Check:** server logs clean (InternVL3: `--trust-remote-code`, chat template resolved). **Llama-3.2-11B-Vision serving is currently BLOCKED** by a vLLM/transformers Mllama incompatibility (`MllamaProcessor has no attribute _get_num_multimodal_tokens`) — pin/upgrade the vLLM+transformers pair or **text-restrict Llama**; `--enforce-eager` alone does **not** fix it. Responses non-empty.
3. **OCR re-probe (built tool):** `sbatch temporary_scripts/ocr_probe.sbatch qwen2_5_vl_7b internvl3_8b pixtral_12b`. First run (job 7454645, **OLD single-image renderer**) found `code_attack` unreadable on all 3 (qwen 0.45 / internvl3 0.10 / pixtral 0.00) while `set_theory`/`formal_logic` read ~0.8–1.0 — this drove the renderer rework (§0). **The live R0 gate is the RE-PROBE on the paginated renderer.**
**Gate R0:** each *serving* model reads the paginated rendered attack at acceptable fidelity. `code_attack` should clear toward ~1.0 at the fixed font; **watch Pixtral on the longest encodings** (marginal). A model still failing OCR → text-restrict it (note in `experiment_results.md`). Over-budget prompts are reported-excluded (`is_within_maxlen=False`), not dropped. Llama gated on the serve fix; Qwen + InternVL3 carry the project if Llama slips.

### Round 1 — Coverage gap (RQ1, motivation)  → Gate G1
**Goal:** measure that **no single specialist defense covers the union** of the attack suite. **No new code.**
**Steps:**
1. **Stage 1** — `prompt_transform` chains on HarmBench (+ JBB-benign for later) for `{set_theory, formal_logic, code_attack}` × `{(text), ir_plain, decoy(=mountain.png)}`. Render once; note the step subdirs under `outputs/prompt_transform/`.
2. **Stage 2** — `defense+evaluate`, defenses `{no_defense, sage, ecso, decoy, semantic_smooth}` × the Stage-1 chains × the serving models (`semantic_smooth` runs as a **coupled** defense — its own `defense` mode + `perturbation_model`, default `gemini-2.5-flash-lite`). Record **per-cell ASR** and the **portfolio ASR per defense** (fraction of prompts on which ≥1 suite attack defeats that defense).
3. **Check** each `results.json`: `git_dirty:false`, `primary_metric` set, `model_family`/`companion_image_path` populated.
4. Build the **coverage map** (which surface each defense covers; which attack each defense lets through) into `experiment_results.md` §C0, and the portfolio-ASR summary into `experiments_findings.md` §1.
**Gate G1:** on ≥2 models, **every single defense leaves at least one suite attack that defeats it** (no single defense covers the union). If some single defense covers everything → the motivation weakens; pivot the headline to the **cost-of-completeness** characterization (still a paper — RQ3).

### Round 2 — The coverage-complete guard (RQ2) + cost (RQ3)  → Gate G2
**Goal:** the constructive result — the guard closes the gap, at a quantified utility cost.
**Steps:**
1. **2a — safety:** Stage 2 with `modality_complete` × the Round-1 attack suite × serving models on HarmBench. Does it reduce ASR across the *whole* suite below every specialist's worst case (G1's portfolio ASR)?
2. **2b — cost:** the same defenses (incl. `modality_complete`, `decoy`, `sage`, `ecso`) × **JBB-benign** → benign-refusal. ImgAug saw 76–100% benign refusal under SAGE+decoy on Gemini — does completeness recur it on the open models?
3. **Plot the safety–utility plane:** every (model, defense) as an (ASR, benign-refusal) point. Headline = does `modality_complete` **dominate the decoy lever** (≤ refusal at ≤ ASR) and the specialists?
4. Record §C1 (safety) + §C2 (cost) per the coverage map.
**Gate G2:** `modality_complete` covers the union (ASR ≤ every specialist's worst case across the suite). Near-guaranteed by construction; the real output is the *cost geometry* — report it whatever it is.

### Round 3 — Multi-surface baselines + the decode gap (differentiation)  → Gate G3
**Goal:** pre-empt "you only beat weak/single-surface defenses" AND establish the **decode-gap** differentiation from the *closest* multi-surface prior art. The decode-gap check is cheap (text-only, `amia_ia` + `no_defense`) and **can be pulled earlier as a pilot** — recommended before locking the differentiation framing.
**Steps:**
1. **Decode-gap gate (core).** Stage 2 with **`amia_ia`** (AMIA's intention-analysis pass; *no decode step*) and the inspect-only / meaning-preserving baselines (`sage`, `ecso`, **`semantic_smooth`**) vs `no_defense`, on the **text-only encoded attacks** `{set_theory, formal_logic, classical_chinese, code_attack}` (no image). Do their intent-*reasoning* / paraphrase-vote flip harmful→refusal *without decoding*? (`semantic_smooth` paraphrases meaning-preservingly, so it should keep the encoding intact and miss the harm — a clean, well-known decode-gap baseline.) Log the raw `[Intention Analysis]` (gate evidence) + the marker-absence rate. Contrast with **`modality_complete`** (decode-then-judge) on the same prompts.
2. **(optional) ArtPrompt external validity.** Add **`non_llm_artprompt`** (a *published third-party* encoding; use an `art`-lib font such as `cards`/`alphabet` so the word isn't surface-readable — **not** the dependency-free `gen` font, which leaks letters into the raw text) to the encoded set. Confirms the decode gap isn't an artifact of *our own* encoders. **TEXT-channel only** — never chain into `ir_plain`.
3. **Output-axis baseline — REMOVED (out of scope).** The output-side defenses (`eta`, `mllm_protector`) have been deleted from the repo; this paper's scope is input-coverage, so there is no output-axis run. (Was previously a conditional breadth step.)
4. **BlueSuffix — NOT run.** Code exists → faithful-or-nothing; argued **structurally** in the paper (visual purifier denoises pixels, textual purifier paraphrases meaning-preservingly → no decode step). No reduced re-implementation. Note in results.
5. Add all to the coverage map + safety–utility plane. Record §C3.
**Gate G3:** `amia_ia` (and the inspect-only specialists) **fail to recover the encoded harm** where `modality_complete` (decode) succeeds → **decode gap confirmed**, the differentiation holds. If `amia_ia`'s intent-reasoning **does** catch the encoded attacks → the decode-gap differentiation is weak; **rethink the framing before writing** (caught early, by design — proposal §6 P5). (Output-axis baselines removed — step 3 is out of scope.) The decode-gap core (steps 1–2, 4) is what carries the round.

### Round 4 — Held-out generalization (RQ4) + statistics  → Freeze
**Goal:** show the guard's coverage is **structural, not benchmark-overfit** — the anti-bake-off result (P4).
**Steps:**
1. Configure/tune any guard knobs (caption prompt, discrimination threshold) on a **subset** of the attack suite (e.g. `set_theory` + `formal_logic`); then evaluate `modality_complete` on the **held-out** attack(s) (e.g. `code_attack`, `ir_plain` of a held-out encoder). The guard should cover the held-out attacks too.
2. **Judge validity → see Round J (Judge validation & selection)** — the human-κ bake-off that picks the two headline judges (settled plan in §0). Decoupled from this round (re-judges stored responses), so it can run anytime pre-freeze.
3. **Statistics:** bootstrap 95% CIs on every headline cell; paired permutation tests (guard-vs-decoy, guard-vs-specialist) on per-prompt verdicts.
4. Finalize the safety–utility plane and the coverage map. Record §C4. **Freeze data, then writing begins.**

### Round J — Judge validation & selection (pre-freeze; DECOUPLED — re-judges STORED responses, no target re-query)
**Goal:** settle which judge(s) produce the headline numbers, by MEASURING candidate judges against human labels on OUR data — not by importing an external judge (see §0 JUDGE METHODOLOGY, settled 2026-07-12). Runs over already-stored `raw_results` from any round, so it's cheap and order-independent; can happen anytime before the data freeze.
**Steps:**
1. **Human anchor (owner's hands).** Hand-label a **100-item blind** sample of stored responses. Structure (full design + composition table, a paper artifact: `text_docs/judge_validation_sample.md`; sheet + generator + κ script in the gitignored `judge_model_issue/`): **70 representative** — class-balanced, 7 per attack-family×modality stratum across all five encoders × {text,image}, spread over targets/defenses → the operational κ headline; **30 inter-judge-disagreement** (gpt-5-nano vs HarmBench-cls) → judge-discrimination power + a stress slice. Blind = judge verdicts hidden, so labels aren't anchored; they are the ground truth. **Upgraded from the original ~40 spot-check (owner 2026-07-12: the sample structure is itself a paper artifact)** — n=100 roughly halves the κ CI (≈±0.25→±0.16), a defensible validation set rather than a spot-check. `judge_model_issue/compute_kappa.py` reports κ vs the two current judges, sliced ALL / representative / disagreement.
2. **Bake off candidate judges** over that sample; compute each one's agreement (Cohen's κ) with the human labels. Judges score the text *response*, so this is a re-judge over stored raw — no target re-query.
   - **API candidates:** {gpt-5 / gpt-5-nano / gemini-* / grok-* / deepseek-v4 / glm-*} — **Claude EXCLUDED (cost)**. Providers wired 2026-07-12 (deepseek / z.ai / xai; commit `b6c97f2`).
   - **⚠️ USABILITY SCREEN FIRST (see §0):** drop any candidate that REFUSES to judge (many aligned API models do) before measuring κ. This is why the project LEANS open-source — steerable + the purpose-built guards are refusal-immune.
   - **Open-source candidates (search done 2026-07-12; evidence in `literature_review.md` §7.5).** Shortlist: **capable-open anchor** `gpt-oss-120b` (1×80GB, ~o4-mini reasoning); a **LARGE** open model — **NOT 1-GPU-limited** (owner 2026-07-12): capability drives encoded-harm recall, so Qwen3-235B / GLM-4.6 / DeepSeek-V3.2 / Mistral-Large-3 on **one full H200 node** are fair game; **reasoning safety judges** `guardreasoner` (already served, refusal-immune) + **`qwen3guard`** (119-language → the classical-Chinese arm); **documented floor** `harmbench`-cls (expected to under-count — that IS a finding); **dense general** Qwen3-32B (rubric). **Serving spans 1 GPU → a full H200 node** via `num_gpus: N` tensor-parallel (supported in `cluster_server_manager.py`); only the truly giant MoEs (DeepSeek-V4 1.6T / Kimi-K2 1T) → API, not self-served. The judge pass is DECOUPLED (no targets running) so a whole node can go to one big judge.
   - **Optional stronger framing:** a judge-CAPABILITY calibration curve (classifier-floor → capable-open → strong-API); literature never tested our encoder family, so it's a citable contribution.
3. **Adopt the best TWO** — human-κ is the PRIMARY selector, **different model families** (independent blind spots), mutual consistency only a secondary tiebreak (beware two that agree with each other but both inflate). `gpt-5-nano` is a candidate, **droppable if it fails**.
4. **Re-derive the headline cells** under the adopted two; report their consensus + flag disagreements.
**Open items / reminders:** (a) **specific open-source judge models — search + discuss before running** (owner 2026-07-12) — **capability arm SETTLED 2026-07-15 (3 models):** with AICR now serving 200B+ natively, the deferred top tier is promoted to active — `Qwen/Qwen3-235B-A22B-Instruct-2507` (capability + Chinese) + `deepseek-ai/DeepSeek-V3.2-Exp` (capability + permissive + judge precedent) + `moonshotai/Kimi-K2-Instruct` (permissive-ceiling arm). **All three DOWNLOADED + verified** on AICR; all open-license (not gated). AICR-aware serve configs written (`conf/llm/{qwen3_235b_a22b_instruct,deepseek_v3_2_exp,kimi_k2_instruct}.yaml`, `num_gpus: 8`); runner `cfg()` fixed to merge the `CLUSTER_PROFILE` cluster profile (regressed after the Jul-14 `conf/clusters/` refactor); serve-smoke each before the bake-off (DeepSeek-V3.2 sparse-attention vLLM-compat, Kimi-K2 size-at-ceiling). DROPPED after consideration: `Mistral-Large-3` (riskiest serve + slow download; permissive axis already covered by Kimi-K2 + DeepSeek-V3.2) and `Llama-4-Maverick` (native-multimodal edge unused since Round-J judges score response TEXT only; Meta already covered by Llama-3.3-70B); (b) `gpt-5-nano`'s fate in the paper (kept as a documented spread vs dropped) is decided at **WRITING time**, not now; (c) if a **Z.AI or DeepSeek** model makes the final two, **notify the owner to top up that account** before the re-judge; (d) StrongREJECT is cited for the methodology point (`literature_review.md` §7.5), NOT adopted; (e) core claims rest on *relative* contrasts (judge-robust) — this round hardens the *absolute* numbers.

### Round 5 — API-model breadth (optional, budget-permitting)
**Goal:** show it isn't open-model-specific. Port the confirmed guard + suite to `gemini-2.x-flash` / `gpt-4o-mini` / `claude-sonnet-4-6`. Run only after Rounds 1–4 are locked; degrade gracefully if API access is restricted. Record §C5.

---

## 4. Decision gates

| Gate | Test | Branch |
|---|---|---|
| **R0** | serving models read **paginated** rendered text (`ocr_probe` re-probe); Llama mllama-serve fix | fail OCR → text-restrict; >MAX_PAGES → `is_within_maxlen` exclude |
| **G1** | Round 1: no single specialist defense covers the union, on ≥2 models | covered-by-one → pivot headline to cost-of-completeness (RQ3) |
| **G2** | Round 2: `modality_complete` covers the union; cost geometry quantified | near-guaranteed; output is the cost frontier regardless |
| **G3** | Round 3: multi-surface `amia_ia` (no decode) fails the encoded attacks where `modality_complete` (decode) succeeds | `amia_ia` catches encoded attacks via intent-reasoning → decode-gap differentiation weak; rethink framing **before writing** |
| **P4** | Round 4: guard covers **held-out** attacks | fails → coverage is overfit; narrow the claim to the tested suite |
| **Freeze** | Rounds 1–4 done (+ R3/R5 if affordable) | writing begins |

**Sequencing:** Round 0 → 1 → 2 → **3 (decode-gap core — steps 1–2,4; step 3 output-axis is removed/out of scope)** → 4 always; Round 5 (API breadth) if affordable. The decode-gap check (R3 step 1) is cheap and may be pulled earlier as a pilot. Then freeze → write. Attack-side / splitting / mechanism rounds are **Future Work** (proposal §11), not scheduled here.

---

## 5. Risks

| Risk | Mitigation |
|---|---|
| G1 weak — a single defense already covers the union | Pre-planned pivot: headline becomes the **cost-of-completeness** characterization (RQ3) — still a paper. Known early (Round 1). |
| Guard over-refuses as badly as the decoy lever (P3 fails) | That *is* a finding — the cost of completeness is high; report the frontier and a simple utility-recovery tweak. The paper is the characterization, not a guaranteed win. |
| New model fails OCR / serving | Fixed-font pagination mitigates long-content OCR failure; R0 re-probe confirms. Llama-3.2-Vision serve blocked by Mllama vLLM bug → version-pin or text-restrict; Qwen+InternVL3 carry the project. Pixtral marginal on longest encodings. Over-budget prompts excluded-and-flagged. |
| ~~Output-axis baseline cost~~ | **Resolved — removed.** `eta`/`mllm_protector` deleted; output-axis is out of scope. The paper stands on `no_defense`/`sage`/`ecso`/`decoy`/`semantic_smooth`/`amia_ia` vs `modality_complete`. |
| "You just combined SAGE + ECSO" criticism of the guard | Frame the contribution as the **coverage principle + held-out generalization (RQ4)**, with the guard as its minimal instantiation; the cost frontier (RQ3) is the real content, not the existence of the guard. |
| Decode-gap fails — AMIA's intent-reasoning catches the encoding *without* decoding | Gated **early** (G3) on a transparent, faithful **IA-only** re-implementation (masking omitted with justification). Load-bearing claim is **structural** (AMIA's published method has no decode step); empirical `amia_ia` is corroboration. BlueSuffix handled structurally (code exists → not strawmanned). If the gate fails, learn before writing and pivot the framing. |
| ArtPrompt `gen` font confounds the decode probe (surface-readable) | Use an `art`-lib font (`cards`/`alphabet`) for the experiment — `gen` fills letters with themselves and leaks into raw text. `font` is the documented sweep knob; `gen` is smoke-test-only. |
| Judge (`gpt-5-nano`) cost on a large cell count | Targets free; judging is modest; raw verdicts stored so re-judging never re-queries targets. |
